# ============================================================================
# EmailHub environment template / EmailHub 环境变量模板
# Copy this file to .env before running.
# 运行前请复制为 .env 并按需修改。
# ============================================================================

# Database / 数据库
DATABASE_URL="file:./dev.db"

# Authentication / 认证
# Generate NEXTAUTH_SECRET:
# Linux/macOS: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-at-least-32-characters-secret-key"

# Encryption / 加密
# Generate ENCRYPTION_KEY:
# Linux/macOS: openssl rand -hex 16
ENCRYPTION_KEY="0123456789abcdef0123456789abcdef"

# Runtime / 运行参数
NODE_ENV="development"
PORT="3000"

# Optional / 可选
LOG_LEVEL="info"
UPLOAD_DIR="./uploads"
MAX_UPLOAD_SIZE="10485760"

# Optional Redis (currently only used by scripts/health-check.ts)
# 可选 Redis（当前仅健康检查脚本会读取）
REDIS_HOST="localhost"
REDIS_PORT="6379"
